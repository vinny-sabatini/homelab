apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- http-route.yaml
- service-monitor.yaml

helmCharts:
  - name: bambuddy
    repo: https://npelikan.github.io/bambuddy-helm
    version: 0.1.2
    valuesFile: values.yaml
    releaseName: bambuddy
